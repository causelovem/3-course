Функции написал Козлов Михаил Владимирович, 323 группа

void ExtractFeatures(const TDataSet& data_set, TFeatures* features)
{
	основная функция (базовая часть), в которой реализованно составление дескриптора фотографии
	и занесение его в основной массив для обучения
}

void LBP (std::vector<float> &descriptor, const Matrix <float> &bright, int block_rows, int block_cols)
{
	функция первого дополнительного задания, вызывается в функции 'ExtractFeatures'
	в ней реализованно дополнение основного дескриптора, путём добавления в него
	дескриптора локальных бинарных шаблонов
	(число в десятичном виде формируется с помощью бинарных сдвигов)
}

void color_feature (std::vector<float> &descriptor, BMP *img)
{
	функция второго дополнительного задания, вызывается в функции 'ExtractFeatures'
	в ней реализованно дополнение основного дескриптора, путём добавления в него
	дескриптора цветовых признаков
}

Дополнительная информация:

Реализован класс 'class UnaryMatrixOp', который нужен для работы у 'unary_map'
(сворачивает часть матрицы с ядром)
Для нормировки дескрипторов взята евклидова норма
Файл с обученной моделью содержится в '/build/bin/model.txt'
