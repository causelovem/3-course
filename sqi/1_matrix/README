Постановка задачи:
Программа, оценивающая влияние кэша на время умножения матриц (последовательный алгоритм).

Для компиляции программы используйте "make"
Для запуска ./matrix <A-file> <B-file> <C-file> <type>,
где <type> 1 - ijk, 2 - ikj, 3 - jki, 4 - jik, 5 - kij, 6 - kji.

Матрицы сгенерированны программой "gen.cpp".
Формат файла матрицы: число (0 - float, 1 - double), " ", размер матрицы (row" "col), "\n", далее сама матрица, записанная построчно (тестовые матрицы находятся в папке test_matrix).

Команда "make report" перемножает матрицы 500х500 и строит график зависимости времени от <type> (файл plot.eps)
Команда "make test" проверяет правильность перемножения матриц пользуясь результатом перемножения пакета "octave".

Исследование показало, что наименьшее время выполнения алгоритма достигается при порядке индексов 2 - ikj(наименьшее количество промахов кэша), а наибольшее 6 - kji(наибольшее количество промахов кэша).

В основной программе реализованно 6 алгоритмов умножения матриц и их обработка (считывание, вывод, определение типа данных).
В программе верификации реализованно умножения матриц пакетом "octave" и их сравнение.

Программы и отчёт написал
Козлов М.В.
323гр.